# Use an official lightweight Nginx image as the base image
FROM nginx:alpine

# Copy the braille_converter.html file into the Nginx default web directory
COPY braille_converter.html /usr/share/nginx/html/index.html

# Expose port 80 in the container to the outside world
EXPOSE 80
